# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
src/data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Agent scratch (plan-runner writes here)
.plan-run

# Rust vendored crates (src-tauri has its own anchored /target/ in src-tauri/.gitignore)
/crates/**/target/
# Cargo.lock IS tracked here: CI runs `cargo test --manifest-path crates/*/Cargo.toml`
# standalone, so without the lock those runs resolve dependencies fresh every time
# and can go red on a day nobody touched this repo.

# Local Codex/Superpowers workspaces and design mockups
.worktrees/
.superpowers/
